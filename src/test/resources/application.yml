server:
  port: 80


build:
  name: ABC Proj
  version: 0.1

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true

jwt:
  secret: f1aPs3cr37
  expire: 5