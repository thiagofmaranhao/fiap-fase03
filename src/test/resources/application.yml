server:
  port: 80

buid.name: ABC Proj
build.version: 0.1


#spring:
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
#    username: sa
#    password: sa

# TODO: Validar com o prof se é para testar na mesma base de dados ou usar o H2
spring:
  datasource:
    url: jdbc:mysql://${DB_HOSTNAME:localhost}:${DB_PORT:3306}/${DB_NAME:abc_tech}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Fiap1234}

  jpa:
    hibernate:
      ddl-auto: none

jwt:
  secret: f1aPs3cr37
  expire: 5